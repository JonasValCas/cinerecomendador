<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title + ' - ' : '' %>Sistema de Recomendacion de Peliculas</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="/css/chat.css">
  <link rel="stylesheet" href="/css/rating-styles.css">
  <%- typeof style !== 'undefined' ? style : '' %>
</head>
<body>
  <%- include('partials/navbar') %>
  
  <div class="container mt-4">
    <%- body %>
  </div>
  
  <% if (locals.currentUser) { %>
  <!-- Botón flotante para abrir el chat -->
  <div class="chat-float-btn" id="chat-float-btn">
    <i class="fas fa-comments"></i>
  </div>
  
  <!-- Chat flotante -->
  <%- include('partials/chat-float') %>
  <% } %>
  
  <footer class="bg-dark text-white text-center py-3 mt-5">
    <div class="container">
      <p class="mb-0">Sistema de Recomendacion de Peliculas &copy; <%= new Date().getFullYear() %></p>
    </div>
  </footer>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/main.js"></script>
  
  <% if (locals.currentUser) { %>
  <!-- Socket.io -->
  <script src="https://cdn.socket.io/4.6.0/socket.io.min.js"></script>
  <!-- Script del chat flotante -->
  <script src="/js/chat-float.js"></script>
  <!-- Metadatos para el chat -->
  <meta name="username" content="<%= locals.currentUser.username %>">
  <% } %>
  
  <%- typeof script !== 'undefined' ? script : '' %>
</body>
</html>
